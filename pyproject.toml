[tool.pytest.ini_options]
pythonpath = "."
markers = [
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]
addopts = [
    "--ignore=tests/integration/test_macro_data_coll.py"
]